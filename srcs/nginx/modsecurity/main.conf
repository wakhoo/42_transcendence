# ============================================================
#  ft_transcendence — ModSecurity entry point
#  Load order matters: base tuning -> CRS setup -> CRS rules ->
#  project-specific exclusions (must come last to win).
# ============================================================

Include modsecurity.conf
Include crs-setup.conf
Include /usr/share/modsecurity-crs/rules/*.conf
Include custom-exclusions.conf